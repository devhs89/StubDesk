type Employee {
    firstName: String
    lastName: String
    age: String
    hireDate: String
    jobTitle: String
    department: String
    employeeType: String
    currentStatus: String
}

type Query {
    allEmployees(employeeType: String): [Employee]
}

type Mutation {
    addEmployee(payload: String!): Employee
}